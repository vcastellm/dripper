#!/usr/bin/env bash

PACKAGE=example
VERSION=1.3.3
DEBUG=true

echo "Package: $PACKAGE"
echo "Version: $VERSION"

if [ ! -z $DRIP_INSTALL ]; then
  echo "=> Install"
fi

if [ ! -z $DRIP_INFO ]; then
  echo "=> Info"
fi
